[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ballsdex-Art"
version = "0.0.0"

description = "Art extensions"
license = "MIT"
authors = [
    { name = "Haymooed, Caylie" },
]
readme = "README.md"

dependencies = [

]